
#include "generator.hpp"

int Generator::generate_rDAG(size_t n, size_t m, bool allow_parallel_edges, size_t d, int **weights, std::ostream out)
{
   return -1;
}

int Generator::generate_rDiGraph(size_t n, size_t m, bool allow_parallel_edges, size_t d, int **weights, std::ostream out)
{
   return -1;
}
